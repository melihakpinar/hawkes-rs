//! Python bindings for `hawk`.
//!
//! Pre-alpha placeholder. The `hawk` crate exposes no algorithms yet (milestone M0
//! ships verification harnesses only), so there is nothing to bind. The PyO3 module
//! and its `maturin` build arrive alongside the first estimator in M1.
